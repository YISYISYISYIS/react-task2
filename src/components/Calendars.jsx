import styled from "styled-components";

const Calendars = () => {
  return <StyledCalendars>Calendars</StyledCalendars>;
};

const StyledCalendars = styled.section`
  background-color: #a5a5a5;
  border-radius: 16px;
  padding: 20px;
`;
export default Calendars;
